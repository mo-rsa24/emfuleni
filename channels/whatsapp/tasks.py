"""Background tasks for the channels.whatsapp app. Enqueued via RQ.

Define tasks here. Other apps enqueue them by calling
channels.whatsapp.services.enqueue_<task_name>(...) — never by importing the task
function directly.
"""
