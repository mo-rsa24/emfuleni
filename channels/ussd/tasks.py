"""Background tasks for the channels.ussd app. Enqueued via RQ.

Define tasks here. Other apps enqueue them by calling
channels.ussd.services.enqueue_<task_name>(...) — never by importing the task
function directly.
"""
